#!/usr/bin/make -f
export DH_VIRTUALENV_INSTALL_ROOT=/usr/lib
export DH_VERBOSE = 1


%:
	dh $@ --with python-virtualenv

override_dh_virtualenv:
	dh_virtualenv --python python3 --install-suffix cuems --use-system-packages